ѕример асинхронного сервера, примающего запросы по tcp порту и складывающего их в очередь.
»з очереди запросы постепеннно достаютс€ и обрабатываютс€ (без возвращени€ результата клиента, такой пример).

¬ качестве исходного канала можно использовать не только tcp, но и rest api и брокер сообщений. ¬ данном случае, чтобы можно было запустить на "голом" компьютере
был выбран обычный tcp слешатель.

»значально иде€ была проекта была создавать поддомены и там обрабатывать запросы. ƒомены можно выгружать и контролировать. ћожно реализовать создание отдельных 
приложений работающих на x32 архитектуре, в то врем€ как основной сервер на x64. Ќо при создании примера был выбран проект .net core, на котором создание поддоменов 
у microsoft ещЄ не реализовано. ѕоэтому в методах обработки команд стоит метод Sleep, имитирующий какую-то нагрузку.

“естов в примере, нет, но есть клиентское приложение, отправл€ющее запросы ввиде текста. 
—начала запускаем Backend, потом ClientsImitation и смотрим что происходит.
